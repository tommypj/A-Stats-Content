{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if echo \"$CLAUDE_TOOL_INPUT\" | grep -qE \"\\.env(\\.local|\\.production)?\\\"\" ; then echo \"BLOCK: Do not edit .env files — they contain secrets\" >&2; exit 1; fi'"
          }
        ]
      }
    ]
  }
}
